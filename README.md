# LoadLink Rwanda

## Database Setup
1. Install PostgreSQL
2. Create a database named `loadlink`
3. Open pgAdmin Query Tool and run the script in `backend/db/database.sql`
4. Copy `backend/.env.example` to `backend/.env` and fill in your values
```
